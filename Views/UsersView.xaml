<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="PosPokemon.App.Views.UsersView"
             Background="{StaticResource AppBackgroundColor}" d:DesignHeight="359.76" d:DesignWidth="667.83">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Volver -->
        <Button Grid.Row="0"
                Content="⬅️ Volver al Dashboard"
                HorizontalAlignment="Left"
                Height="40"
                Padding="15,0"
                Margin="0,0,0,15"
                Style="{StaticResource PrimaryButton}"
                Command="{Binding BackToDashboardCommand}"/>

        <!-- Header -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="👥 Usuarios"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="{StaticResource PrimaryTextColor}"/>

            <Button Grid.Column="1"
                    Content="➕ Nuevo"
                    Height="45"
                    Padding="20,0"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding CreateUserCommand}"/>

            <Button Grid.Column="3"
                    Content="🔄 Recargar"
                    Height="45"
                    Padding="20,0"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding LoadCommand}"/>
        </Grid>

        <!-- Tabla -->
        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="12">
            <Border.Effect>
                <DropShadowEffect Color="{StaticResource ShadowColor}" Opacity="0.08" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>

            <DataGrid ItemsSource="{Binding Users}"
                      SelectedItem="{Binding SelectedUser}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      BorderThickness="0"
                      RowHeight="42">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                    <DataGridTextColumn Header="Usuario" Binding="{Binding Username}" Width="*"/>
                    <DataGridTextColumn Header="Rol" Binding="{Binding Role}" Width="120"/>
                    <DataGridTextColumn Header="Activo" Binding="{Binding IsActive}" Width="90"/>
                    <DataGridTextColumn Header="Creado (UTC)" Binding="{Binding CreatedUtc}" Width="220"/>

                    <!-- Acciones -->
                    <DataGridTemplateColumn Header="Acciones" Width="320">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                                    <Button Content="✏️ Editar"
                                            Margin="4,0"
                                            Padding="12,6"
                                            Style="{StaticResource PrimaryButton}"
                                            Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                            CommandParameter="{Binding}"/>

                                    <Button Content="🔁 Activar/Desactivar"
                                            Margin="4,0"
                                            Padding="12,6"
                                            Style="{StaticResource PrimaryButton}"
                                            Command="{Binding DataContext.ToggleActiveCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                            CommandParameter="{Binding}"/>

                                    <Button Content="🔐 Reset Clave"
                                            Margin="4,0"
                                            Padding="12,6"
                                            Style="{StaticResource PrimaryButton}"
                                            Command="{Binding DataContext.ResetPasswordCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
