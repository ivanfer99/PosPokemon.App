<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="PosPokemon.App.Views.SettingsView"
             Background="{StaticResource AppBackgroundColor}" d:DesignHeight="875.576" d:DesignWidth="821.278">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Volver -->
        <Button Grid.Row="0"
                Content="⬅️ Volver al Dashboard"
                HorizontalAlignment="Left"
                Height="40"
                Padding="15,0"
                Margin="0,0,0,15"
                Style="{StaticResource PrimaryButton}"
                Command="{Binding BackToDashboardCommand}"/>

        <!-- Header -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="⚙️ Configuración"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="{StaticResource PrimaryTextColor}"/>

            <Button Grid.Column="1"
                    Content="🔄 Cargar"
                    Height="45"
                    Padding="20,0"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding LoadCommand}"/>

            <Button Grid.Column="3"
                    Content="💾 Guardar"
                    Height="45"
                    Padding="20,0"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding SaveCommand}"/>
        </Grid>

        <!-- Form -->
        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="20">
            <Border.Effect>
                <DropShadowEffect Color="{StaticResource ShadowColor}" Opacity="0.08" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Nombre de la tienda" FontWeight="Bold"/>
                <TextBox Grid.Row="1" Height="40" Margin="0,6,0,12"
                         Text="{Binding StoreName, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="2" Text="Símbolo de moneda (S/ o $)" FontWeight="Bold"/>
                <TextBox Grid.Row="3" Height="40" Margin="0,6,0,12"
                         Width="120"
                         Text="{Binding CurrencySymbol, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="4" Text="Stock mínimo (alerta bajo stock)" FontWeight="Bold"/>
                <TextBox Grid.Row="5" Height="40" Margin="0,6,0,12"
                         Width="120"
                         Text="{Binding LowStockThreshold, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="6" Text="Nota por defecto en ventas (opcional)" FontWeight="Bold"/>
                <TextBox Grid.Row="7" Height="80" Margin="0,6,0,12"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Text="{Binding DefaultSaleNote, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="8"
                           Foreground="{StaticResource SecondaryTextColor}"
                           FontWeight="SemiBold"
                           Text="{Binding StatusMessage}"
                           Margin="0,10,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
